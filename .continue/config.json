{
  "models": [
    {
      "title": "GPT-5 Mini (Preview)",
      "provider": "openai",
      "model": "gpt-5-mini",
      "apiKey": "${OPENAI_API_KEY}"
    },
    {
      "title": "GPT-4o Mini",
      "provider": "openai",
      "model": "gpt-4o-mini",
      "apiKey": "${OPENAI_API_KEY}"
    }
  ],
  "embeddings": {
    "provider": "openai",
    "model": "text-embedding-3-small",
    "apiKey": "${OPENAI_API_KEY}"
  },
  "allowAnonymousTelemetry": false
}
